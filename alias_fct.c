#include "shell.h"

/**
 * alias_fct - Implement the alias builtin command
 * @commande: array of arguments
 */

void alias_fct(char **commande)
{
	char *dir = commande[1];

	if (dir == NULL)
	{

	}
}
